# language-dictionary-client

A Java frontend application for a dictionary/vocabulary system.

## Overview
This repository is the client-side counterpart to the dictionary server project. It likely handles lookup, user interaction, and communication with the backend service.
